import { StyleSheet } from "react-native"

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  errorImage: {
    maxWidth: "40%",
  },
  header: {
    fontWeight: "bold",
    fontSize: 20,
    marginBottom: -50,
  },
  subHeader: {
    marginTop: -50,
    fontSize: 20,
    color: "#666",
  },
})
