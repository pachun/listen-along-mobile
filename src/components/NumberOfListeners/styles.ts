import { StyleSheet } from "react-native"

export default StyleSheet.create({
  numberOfListeners: {
    color: "#999",
    fontSize: 12,
    marginRight: 15,
    marginTop: 4,
  },
  numberOfListenersContainer: {
    position: "absolute",
    bottom: 10,
    right: 10,
  },
})
