import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  navBarButton: {
    paddingLeft: 10,
    paddingRight: 100,
    paddingTop: 10,
    paddingBottom: 10,
  },
})

export default styles
